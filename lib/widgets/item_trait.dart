import 'package:flutter/material.dart';

class ItemTrait extends StatelessWidget {
  const ItemTrait({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}